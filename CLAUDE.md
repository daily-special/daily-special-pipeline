# daily-special-pipeline

「오늘의 정식」(Daily Special) — 모험가 길드의 가성비 구내식당을 운영하는 코지 경영 시뮬 게임. **이 repo는 그 게임의 AI 콘텐츠 생성 파이프라인(Python)이다.**

**작업 전에 `docs/`를 읽는다.** 이 파일은 그 요약과 작업 규칙이다.

| 문서 | 언제 읽나 |
|---|---|
| `docs/design.md` | 항상. 게임 전체 설계와 결정 근거 |
| `docs/conventions.md` | **코드를 쓰기 전에 항상.** 계층·스키마·LLM·검증·테스트 규약. 이 문서가 코드를 강제한다 |
| `docs/data-contract.md` | 출력 JSON을 건드릴 때. 서버·클라와의 유일한 접점 |
| `docs/roadmap.md` | **작업 시작 전에 항상.** 작업 순서·끊는 선·현재 위치. 진행하면 갱신한다 |

---

## 대회 (NAN 2026)

NHN 게임×AI 해커톤, 채용 연계형. **마감 8/10, 혼자, 18일.** 예선 제출물: 게임 빌드·시연 영상·게임 소개서·**AI 활용 기술 문서**. 본선(9/4~6): 프로토타입·**에이전트 설계서**·**디렉팅 명세서**.

**이 파이프라인이 대회의 AI 쇼케이스다.** 「AI 활용 기술 문서」의 알맹이가 여기서 나온다.

---

## 저장소 구조 (물리 분리, 3 repo)

```
daily-special-pipeline   Python AI 콘텐츠 생성   (우분투) ← 여기
daily-special-server     Spring Boot 런타임      (우분투, 배포 대상) — 서버 작업 때 생성
daily-special-client     Unity 도트 2D          (윈도우) — 스파이크 후 생성
```

셋은 **데이터 계약으로만** 만난다 — 파이프라인이 콘텐츠를 데이터(JSON/에셋)로 뱉고, 서버·클라가 소비. 코드 공유 없음.

`vtuber-ip-copilot`(다른 repo)은 **건드리지 않는다** — 친구와의 미래 게임용. 여기선 그 **패턴만 복사**해 온다.

---

## 이 파이프라인이 하는 일

게임 콘텐츠를 **오프라인 생성**한다 — 손님 페르소나(+행동 파라미터), 요리, 재료, 대사. 생성 → 검증 → 재생성 → 데이터 패키지 출력.

**핵심: 하이브리드 손님 에이전트.**

```
[오프라인, LLM]  페르소나 생성 = 정체성·말투·행동 파라미터
       ↓ 컴파일
[인게임, 결정적] 손님이 상태로 욕구 표현 → 요리 평가 → 반응.  런타임 LLM 0
```

**게임 런타임에서 LLM을 호출하지 않는다.** LLM이 만든 페르소나를 결정적 정책으로 컴파일해 추가 추론 비용을 0으로 만드는 것이 차별점이자 기술 문서의 핵심. (상세: design.md 5장)

---

## 재사용할 패턴 (vtuber-ip-copilot에서)

- **게임 수치를 코드에 박지 않는다.** 스탯·범위·어휘·수량은 `ProjectBible` 설정 데이터가 소유하고, LLM 프롬프트도 거기서 생성. 스키마의 소비자가 코드 + 프롬프트 둘이라 변동 축을 코드로 조이면 변경 비용을 두 배로 낸다
- **구조화 출력**: OpenAI `.parse()`. strict 모드가 임의 키 dict를 금지하므로 **ProjectBible에서 스키마를 동적 생성**(스탯 하나가 필드 하나, 어휘가 enum). 수치 제약(min/max/minItems)은 거부될 수 있어 설명으로 알리고 `check()`가 검증
- **검증은 예외가 아니라 `list[Issue]`로 모아 돌려준다.** 단 설정(ProjectBible)의 불변식은 생성 시점에 예외로 막는다
- **재생성 루프**: 규칙 위반 → 피드백 실어 재생성(최대 3회). 소진돼도 버리지 않고 경고와 함께 낸다. 규칙 깨진 것은 의미 검토에 안 넘긴다
- **검토 계층**: 판정 + 근거 + 수정 방향을 구조화 출력으로, `FAST` 티어, 검토 실패는 생성물을 버리지 않음
- 스택: Python 3.13 / uv / Pydantic / OpenAI SDK. 모델은 티어(QUALITY/FAST)로 말하고 어댑터가 모델명으로 변환. 헥사고날-Lite (domain 순수, application 포트, adapter 구현)

---

## 코드 규칙

- 주석·독스트링·에러 메시지는 **한국어**, 식별자는 영어
- mypy strict · ruff · pytest 전부 통과해야 커밋. 테스트는 가짜 어댑터로 API 키 없이 통과하게
- 설계 결정은 주석이 아니라 **테스트로 고정**
- **저장소 문서는 외부(Notion·위키)를 참조하지 않는다** — 자체 완결로 쓴다

## 작업 방식

**작업 단위마다 아래 5단계를 돈다.** 단위의 기본 크기는 `docs/roadmap.md`의 한 단계다. 그보다 크면 쪼개서 각각 루프를 돈다.

### 1. 사전 보고

- **무엇을** 만드는가 — 한두 문단
- **어디를** 건드리는가 — 새로 만들 파일·고칠 파일 목록
- **무엇을 안 하는가** — 이번 범위 밖. 이걸 적어야 범위가 조용히 넓어지지 않는다

### 2. 의사결정 필요 사항 체크

- 사용자의 판단이 필요한 것**만** 올린다. 관례적 기본값이 있으면 **권장안을 먼저 제시**하고 근거 한 줄을 붙인다
- 결정할 게 없으면 **"결정 필요 없음"이라고 명시**한다. 형식을 채우려고 없는 질문을 만들지 않는다
- 게임 수치가 미확정이면 여기서 확인한다

### 3. 컨펌 후 작업

- **컨펌 전에 코드를 쓰지 않는다.**
- 파일을 쓰거나 고칠 때마다 **그 행동을 한 줄로 해설한다** — 어떤 파일에 / 무엇을 / 왜. 도구 출력을 그대로 붙이지 않는다
- 작업 중 계획이 틀어지면 계속 진행하지 말고 **그 자리에서 보고**한다

### 4. 사후 보고

- 한 것 / **안 한 것과 그 이유**
- 품질 게이트 결과. **실패했으면 실패했다고 쓰고 출력을 함께 보인다.** 통과한 것처럼 넘기지 않는다
- 주요 코드는 응답에 함께 보인다
- 작업 중 **내가 판단한 것** 중 사용자가 확인해야 할 것

### 5. 문서 반영

- `docs/roadmap.md` 현재 위치·체크박스 갱신
- 새 결정이 생겼으면 `conventions.md` 또는 `data-contract.md`에 반영
- 반영할 게 없으면 "없음"이라고 말한다

### 브랜치와 PR

**roadmap 한 단계 = 작업 브랜치 하나 = PR 하나.** `dev` 브랜치는 두지 않는다 — 배포가 없어서 main/dev 구분이 아무것도 나누지 못한다.

- 브랜치명 `<type>/<영문-슬러그>` (예: `feat/project-bible`). `type`은 커밋 규약과 같은 목록을 쓴다
- **3단계(작업) 착수 전에 브랜치를 판다.** main에서 작업하지 않는다
- 5단계(문서 반영)까지 끝나면 PR을 올리고, **CI가 초록인 것을 확인한 뒤** squash 머지
- 작업 계획은 `docs/roadmap.md`가 소유한다. GitHub 이슈로 옮기지 않는다 — 두 곳이 되면 어긋난다

커밋은 사용자가 한다. 요청받지 않으면 커밋하지 않고, 커밋 메시지는 응답에 적어준다.

커밋 메시지·명령어 등 사용자가 가져다 쓸 것은 **응답에 직접** 적는다(Bash 출력 X).
커밋 메시지는 `<type>: <한국어 설명>` 제목 한 줄, **50자 이내**. 상세는 `docs/conventions.md` 11절.

게임 수치가 미확정이면 그럴듯한 값을 지어내지 말고 확인한다. 가정값은 설정 데이터 한 곳에만 넣고 가정임을 명시.

---

## 현재 상태

**작업 순서와 현재 위치는 `docs/roadmap.md`가 소유한다.** 여기에 중복해 적지 않는다 — 두 곳에 두면 반드시 어긋난다.

- **설계·규약 확정, 코드 0줄** (roadmap 0단계 완료)
- **병렬(윈도우, 본인)** — Unity APK 스파이크로 go/no-go 확인. 막히면 클라는 웹으로

18일 일정과 미해결 항목은 design.md 14·15장.
